"use client";
import React from "react";

const PendingOrders: React.FC = () => {
  return (
    <div>
      <h2 className="text-xl font-semibold mb-2">Đơn hàng chờ xác nhận</h2>
      {/* TODO: Render danh sách */}
    </div>
  );
};

export default PendingOrders;
